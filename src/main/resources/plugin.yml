name: SimpRewards

main: Simpmc.Rewards.SimpRewardsPlugin

version: 1.0

api-version: "1.21"

folia-supported: true

authors:
  - SimpMC, Minecraft0122

depend:
  - Vault

softdepend:
  - PlaceholderAPI

commands:

  reward:
    aliases:
      - daily

  simprewardsreload:

permissions:

  simprewards.admin:
    default: op
